<%= form_for(@admin_product_category) do |f| %>
  <% if @admin_product_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_product_category.errors.count, "error") %> prohibited this admin_product_category from being saved:</h2>

      <ul>
      <% @admin_product_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :name_category, class: 'form-control', placeholder: 'Enter Category' %>
  </div>

  <div class="actions">
    <%= f.submit 'Save', class: 'btn btn-default navbar-btn' %>
  </div>
<% end %>
