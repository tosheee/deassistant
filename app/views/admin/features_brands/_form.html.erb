<%= form_for(@admin_features_brand) do |f| %>
  <% if @admin_features_brand.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_features_brand.errors.count, "error") %> prohibited this admin_features_brand from being saved:</h2>

      <ul>
      <% @admin_features_brand.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_id %><br>
    <%= f.number_field :product_id %>
  </div>
  <div class="field">
    <%= f.label :brand %><br>
    <%= f.text_field :brand %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
